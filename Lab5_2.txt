#include <string>
#include <iostream>
#include <fstream>
#include <sstream>
#include <algorithm>

using namespace std;

struct smth {

	float s = 0;

	string imya = " ";

	string otch = " ";

    string fam = " ";

	string nazvanie = " ";

	int stoimost = 0;

};

bool cmpByFam(const smth& r1, const smth& r2)

{

	return r1.fam < r2.fam;

}

int main()

{

	setlocale(LC_ALL, "rus");

	string x;

	smth s;

	ifstream file;

	file.open("C://prog//knigi.txt");

	int len(0);

	if (file.is_open())
	{

		while (!file.eof()) 
		{

			string s;

			getline(file, s);

			len++;

		}

	}

	smth* arr = new smth[len];

	file.seekg(0, ios_base::beg);

	for (size_t i = 0; i < len; i++)

	{

		getline(file, x);

		istringstream iss(x);

		iss >> s.s >> s.imya >> s.otch >> s.fam >> s.nazvanie >> s.stoimost;

		arr[i] = s;

	}

	cout << "Информация о всех вязтых книгах: \n" << endl;

	for (size_t n = 0; n < len; n++)

	{

		cout << "Номер билета : " << arr[n].s << "  "

			<< "Имя : " << arr[n].imya << "  "

			<< "Отчество: " << arr[n].otch << "  "

			<< "Фамилия : " << arr[n].fam << "  "

			<< "Название : " << arr[n].nazvanie << "  "

			<< "Стоимость : " << arr[n].stoimost << endl;

	}

	cout << "\nИнформация о книгах автора Набокова :\n" << endl;

	for (size_t n = 0; n < len; n++)

	{

		if (arr[n].fam == "Nabokov")

		{

			cout << "Номер билета : " << arr[n].s << "  "

				<< "Имя : " << arr[n].imya << "  "

				<< "Отчество: " << arr[n].otch << "  "

				<< "Фамилия : " << arr[n].fam << "  "

				<< "Название : " << arr[n].nazvanie << "  "

				<< "Стоимость : " << arr[n].stoimost << endl;

		}

	}